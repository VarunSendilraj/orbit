{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "commandbar"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-current-monitor",
    "core:window:allow-set-size",
    "core:window:allow-set-position"
  ]
}
